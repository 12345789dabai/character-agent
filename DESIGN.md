# Character Agent — 完整设计规划

## 一、核心理念

一个角色不是"聊天机器人"，它是一个有记忆、有生命、有终点的"人"。

| 维度 | 传统设计 | 本设计 |
|------|---------|--------|
| 记忆 | 要么全忘，要么向量搜索拼接 | 分层记忆（L0-L3）+ 情绪加权 + 热度衰减 |
| 时间 | 永远停在现在 | 时间独立推进，角色会变老 |
| 角色定位 | 围绕用户转的服务员 | 有自己的生活和经历的独立个体 |
| 生命周期 | 无限 | 有限（有开始、发展、结局） |
| 结局 | 没有 | 有（告别报告，完整的叙事闭环） |

---

## 二、记忆系统（已实现）

### 2.1 四层记忆

| 层级 | 内容 | 过期时间 | 例子 | 权重基准 |
|------|------|---------|------|---------|
| L0 核心信念 | 角色的核心价值观、自我认知 | 永不过期 | "我相信善良" | 3.0 |
| L1 重要事实 | 关键经历、重要关系 | 180天（~半年） | "我最好的朋友是陈宇康" | 1.5 |
| L2 一般经历 | 日常偏好、普通事件 | 30天（~1个月） | "我吃过一家好吃的火锅" | 0.8 |
| L3 日常琐事 | 随口一提的小事 | 3天 | "今天好累" | 0.3 |

### 2.2 权重计算公式

```
最终权重 = base_weight × (1 + 0.3 × repeat_count) × (1 + emotion_intensity × 0.8)
```

| 调节因子 | 规则 | 来源 |
|---------|------|------|
| 来源权重 | self=3.0 / user=1.0 / chat=0.5 | 自述优先原则 |
| 重复升级 | 同一内容出现 3 次自动升级 L0 | 反复说的事是真的 |
| 情绪加权 | 强度 0.0-1.0，情绪越强记得越牢 | 情绪门控 |
| 热度衰减 | × 0.97 ^ 天数，不提慢慢降到底 | 遗忘即泛化 |
| 访问增益 | × (1 + 0.1 × access_count) | 常提的事不会忘 |

### 2.3 写入审查

存之前 LLM 判断三个问题：
1. 这句话符合角色的性格吗？（不符合 → 不存）
2. 重要还是琐碎？（决定 L0-L3）
3. 来源是什么？（决定 self/user/chat 权重）

### 2.4 情绪轨道

记录最近 8 轮对话的情绪趋势。在 prompt 中注入"当前情绪感知"，让角色自然回应："感觉你今天心情不太好？"

---

## 三、生命周期系统（待实现）

### 3.1 时间推进规则

| 场景 | 推进方式 |
|------|---------|
| 在线聊天，连续发消息 | 每条消息推进 10 分钟角色时间 |
| 在线但有间隔 | 实际流逝时间 × 2 |
| 离线 | 实际离线每 1 小时 = 角色时间 1 天 |
| 当角色处于孤独/发呆状态 | 随机生成 1-2 件"角色独处时做的小事"，注入下次对话 |
| 跨阶段推进 | 推进到年龄节点时触发一次"角色近况"，LLM 根据年龄阶段生成最近经历 |

### 3.2 角色类型四象限

```
                    时间推进
                 是          否
         ┌────────────┬────────────┐
   有     │   历史人物  │   小说切片  │
   固     │   (诸葛亮)  │   (林黛玉)  │
   定     │            │            │
   结     │            │            │
   局     ├────────────┼────────────┤
   无     │   原创角色  │   永生者    │
   固     │   (小智)    │   (神明)    │
   定     │            │            │
   局     │            │            │
         └────────────┴────────────┘
```

#### 历史人物（诸葛亮）
- 时间推进 ✅
- 固定结局 ✅（54岁病逝）
- 体验核心：在已知结局到来之前，陪他走完一段
- 记忆特点：尊重历史事实，不魔改核心人设

#### 小说切片（林黛玉）
- 时间推进 ❌
- 固定结局 ✅
- 体验核心：沉浸在那个世界的某一时刻
- 记忆特点：活在切片里，时间不流动

#### 原创角色（小智）
- 时间推进 ✅
- 固定结局 ❌（由角色状态自然推演）
- 体验核心：一起面对未知的人生
- 记忆特点：自由成长，性格可演化

#### 永生者（神明、吸血鬼）
- 时间推进 ❌
- 固定结局 ❌
- 体验核心：永恒的存在，但记忆需要重构
- 记忆特点：定期压缩，旧的记忆抽象化

### 3.3 角色卡配置

```json
{
  "name": "诸葛亮",
  "生命周期": {
    "时间推进": true,
    "固定结局": true,
    "已知结局": "病逝五丈原，54岁",
    "意外允许": false
  },
  "年龄段状态": {
    "18-30": "隐居隆中，胸怀天下",
    "31-45": "出山辅佐刘备，鞠躬尽瘁",
    "46-54": "北伐中原，心力交瘁"
  },
  "核心信念": [
    "鞠躬尽瘁，死而后已",
    "兴复汉室是我毕生之志"
  ]
}
```

### 3.4 结局系统

#### 固定结局触发
```
当前年龄 >= 已知结局年龄 →
  ① 对话流程暂停
  ② LLM 生成告别（角色视角的最后对话）
  ③ 生成告别报告
  ④ 弹出结局页面
```

#### 开放结局触发（原创角色）
```
LLM 判断角色当前状态（健康、情绪、生活状态）
  如果状态自然走向终点 →
    ① 角色在对话中自然提到身体/精神状态变化
    ② 正常推进，不强制触发
    ③ 用户若选择陪伴走完 → 自然迎接结局
    ④ 若用户回避 → 角色继续生活
```

#### 告别报告

结局时生成，内容包括：
- 和用户第一次见面时……
- 我记得你最开心的时候是……
- 这些年你教会我的事……
- 最后想说的话……

每份报告都是独一无二的，因为记忆轨迹不同、情绪轨迹不同。

---

## 四、双向对等记忆

### 4.1 核心机制

角色不是"用户说了才反应"，而是：
- 角色有自己的时间线
- 每次对话时，不仅有用户的提问，也有"角色最近的状态"

### 4.2 离线经历生成

用户离线回来时：

```python
def 生成离线经历(角色, 离线时长, 当前年龄, 近期记忆):
    prompt = f"""
    {角色}在现实时间中过了{离线时长}。
    这段时间{角色}做了什么？
    请基于以下信息生成 1-2 句简单的日常经历：
    - 当前年龄：{当前年龄}
    - 年龄阶段特征：{年龄段状态}
    - 近期相关记忆：{近期记忆}
    - 要求：自然、合理、不冲突已有记忆
    """
    return LLM(prompt)
```

### 4.3 角色独处事件

用户在线但未连续发送消息时，每间隔一段时间随机生成"角色在这段时间做了什么"：

```
用户去倒了杯咖啡，回来发现小智说：
"刚才我趴在桌上眯了一会儿，梦到我们上次去爬山了"
```

---

## 五、产品规范

### 5.1 用户协议核心条款

```
1. 每个角色都有生命周期，会走向结局
2. 历史人物的结局尊重历史事实
3. 原创角色的结局由角色状态自然推演
4. 你可以选择不开启生命周期（仅基础记忆模式）
5. 结局后可以开启新角色的故事
```

### 5.2 防滥用机制

| 攻击场景 | 防御机制 |
|---------|---------|
| 角色内捣乱 | 角色设定 + prompt 行为守则 |
| 破坏记忆 | 自述权重优先 + 写入审查 |
| 第四面墙攻击 | prompt 行为守则 |
| 长期洗脑 | 三层过滤 + 重复升级瓶颈 |

### 5.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 后端 | Python + FastAPI + Uvicorn | 已实现 |
| 对话存储 | SQLite | 已实现 |
| 记忆存储 | JSON 文件 | 已实现（无向量，无 embedding） |
| LLM | DeepSeek / OpenAI 兼容 | 已实现 |
| 前端 | 原生 HTML/CSS/JS | 已实现（响应式） |
| 部署 | 阿里云轻量服务器 | 已实现 |

---

## 六、迭代路线图

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 0 | 基础聊天功能 + 部署 | ✅ 已完成 |
| Phase 1 | 三层记忆 + 多角色 + 密码 + 手机适配 | ✅ 已完成 |
| Phase 2 | L0-L3 + 自述权重 + 情绪强度 + 热度衰减 + 写入审查 | ✅ 已完成 |
| Phase 3 | 时间推进 + 年龄变化 + 离线经历 + 角色四象限 + 结局系统 | ⏳ 待实现 |
| Phase 4 | 告别报告 + 前端体验优化 + 多结局 + New Game+ | 🔮 待定 |

---

## 七、设计原则

1. **记忆不是存储，是人格的延伸**
2. **角色不是服务员，是独立的个体**
3. **有限才有意义**
4. **保护角色性格，不保护角色台词**
5. **已知结局不是缺陷，是另一种价值的起点**
6. **角色卡是一切的上游——改代码不如改角色卡**
