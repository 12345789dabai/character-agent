# Character Agent — 项目总结报告

## 一、项目定位

一个有"记忆"和"生命"的 AI 角色系统。不是永无止境的聊天机器人，而是在有限互动中建立真实情感连接的 AI 角色体验。

## 二、核心理念

### 2.1 记忆不是存储，是人格的延伸

AI 角色之所以让人感觉"假"，核心原因不是它不会聊天，而是它不记得。传统的角色对话系统要么没有记忆（每次都是初见的陌生人），要么用向量搜索强行拼接（搜到就是记住，搜不到就是没发生过），这两种都不像人。

本项目采用的方案是：**分层记忆 + 情绪加权 + 热度衰减**。

- 最重要的信息永久保留（L0 核心信念）
- 重要事实保留半年（L1）
- 一般经历保留一个月（L2）
- 日常琐事保留三天（L3）
- 不提的事慢慢"降温"，提到又重新"热起来"
- 情绪强烈的事记得更牢

### 2.2 角色不是服务员，是独立的"人"

当前所有 AI 角色产品的共病：角色永远围着用户转，没有自己的生活。用户来了就陪聊，用户走了就消失。这不是一个"人"。

本项目的方向是：**角色有自己的时间线、自己的经历、自己的人生**。用户在聊天时，角色不仅是在听，也在分享自己当前的生活状态。用户离线时，角色的生活仍在继续。

### 2.3 有限才有意义

无限的生命消解一切重量。如果小智永远不会老、不会死、永远不会离开，那今天聊和十年后聊有什么区别？但如果有结局，每一次对话都有了分量。

本项目的核心设计：**每个角色有完整的生命周期**，从相遇走向告别。用户和角色共同经历一段完整的人生，而不是无尽的日常。

## 三、产品形态

这是一款"AI 角色扮演游戏"，而不是一个聊天工具。

| 维度 | 传统 AI 聊天 | 本项目 |
|------|------------|--------|
| 核心体验 | 问答/服务 | 角色扮演/情感陪伴 |
| 角色 | 工具/助手 | 有独立人格的"人" |
| 记忆 | 偶尔记得/经常忘记 | 分层记忆+情绪加权+热度衰减 |
| 生命周期 | 无限 | 有限（从相遇到告别） |
| 用户关系 | 用户使用产品 | 用户陪伴角色走完一生 |
| 结局 | 无 | 有（完整的故事闭环） |

## 四、技术架构

### 4.1 记忆系统（已实现）

```
L0 核心信念
  └─ 角色的核心价值观、自我认知（永不过期）
L1 重要事实
  └─ 关键经历、重要关系（半年过期）
L2 一般经历
  └─ 日常偏好、普通事件（30天过期）
L3 日常琐事
  └─ 随口一提的小事（3天过期）
```

每层之上叠加：
- **自述权重优先**：角色自己说的话权重 3.0，用户说的 1.0，闲聊 0.5
- **重复升级**：同一内容出现 3 次自动升级到上一级
- **情绪加权**：开心/难过的事记得更牢（权重 × 1+情绪强度）
- **热度衰减**：不提的事慢慢降温，提到又热起来，降到底不再出现在 prompt 但不删除
- **写入审查**：存之前检查是否符合角色性格，防止一次口误导致永久错误记忆

### 4.2 对话系统

```
一次对话的完整流程：

1. 时间推进 → 计算用户离线时长，推进角色时间和年龄
2. 记忆提取 → 获取所有有效记忆，按热度降序排列
3. 情绪感知 → 读取最近几轮的情绪趋势
4. 组装 prompt → 角色设定 + 当前年龄 + 有效记忆 + 情绪感知
5. LLM 回复 → 流式输出
6. 记忆更新 → 后台：情绪标记 → 记忆提取 → 去重升级 → 热度更新
```

### 4.3 存储

| 数据 | 存储方式 | 说明 |
|------|---------|------|
| 对话历史 | SQLite | 最近 N 轮对话，用于展示 |
| 长期记忆 | JSON 文件 | 每角色一个文件，L0-L3 + 日志 + 情绪轨道 |
| 角色卡 | JSON 文件 | 角色设定、性格锚点、年龄分段 |
| 用户配置 | JSON 文件 | API Key、模型等 |

## 五、技术栈

| 层 | 技术 |
|----|------|
| 后端框架 | Python 3 + FastAPI + Uvicorn |
| 对话存储 | SQLite |
| 记忆存储 | JSON 文件（纯文本，无向量，无 embedding） |
| LLM API | DeepSeek / OpenAI 兼容 |
| 前端 | 原生 HTML/CSS/JavaScript（响应式布局） |
| 部署 | 阿里云轻量服务器（Ubuntu） |
| 版本控制 | Git + GitHub |

## 六、当前功能清单

- [x] 分层记忆系统（L0-L3 + 情绪 + 热度）
- [x] 多角色切换
- [x] 角色创建/编辑/删除
- [x] AI 联网生成角色
- [x] 流式对话
- [x] 访问密码保护
- [x] 手机适配
- [x] 记忆可视化面板（五个 tab + 权重/情绪显示）
- [x] 阿里云部署

## 七、待实现

- [ ] 生命周期（时间推进 + 年龄变化 + 离线推进）
- [ ] 双向对等记忆（角色有自己的生活经历）
- [ ] 结局系统（回顾、留言、多结局）
- [ ] 多轮记忆整合压缩
- [ ] 用户登录系统（多用户隔离）

## 八、项目信息

- **线上体验**：http://39.106.182.113:8000（密码：20041209）
- **GitHub**：https://github.com/12345789dabai/character-agent
- **候选人**：陈宇康 · 南京林业大学 · 2027 年毕业
- **求职意向**：杭州 · AI 产品运营 / AI Agent 开发 实习生
